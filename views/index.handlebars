<div style="position: relative; left: 400px;">
  <h1 style="position: relative; left: 140px;">Eat Da Burger!</h1>
  <img id="burger" style="height: 200px; position: relative; left: 140px;" src="https://media.giphy.com/media/yYc5L3vhTWeBi/giphy.gif">
  <div id="burger-field">

    {{#each burgers}}
    <div class="division" style=>
      <p>{{this.burger_name}}</p>
      <button class="devour" id={{this.id}}>
        Devour!
      </button>
    </div>

    {{/each}}
  </div>
  <input id="input" style="position: relative; top: 50px; left: 150px;" type="text" rows="4" cols="50" name="fname" placeholder="Enter Burger Name">
  <br>

  <button id="button" style="position: relative; top: 70px; left: 200px;">
    Burger Button
  </button>
</div>

<script>

  $(document).ready(function () {
    //var burgerIteration = 1;


    //burgerIteration = burgerIteration + 1;
    //var burgerName = document.getElementById("input").value;
    //var burgerId = ("#burgerId") + (burgerIteration);
    //console.log(("Burger Iteration: ") + burgerIteration);

    //console.log(("Burger Name: ") + burgerName);
    //console.log("Burger ID: " + burgerId);

    //$("#burger-field").append(("<div style='background-color:red; width:250px;'>") + ("<h2>") + burgerName + ("</h2>") + ("<button id='devour'>") + ("Devour It!") + ("</button>") + ("</div>")).attr('id', 'newID');
    //console.log($);
    $("button.devour").on("click", function () {
      $.ajax({
        method: "PUT",
        // url: "/api/devour" + $(). 
        url: "/api/devour/" + $(this).attr("id"),
        data: { devoured: "1" }
      })
        .done(function (msg) {
          //alert("Data Saved: " + msg);
          console.log(msg);
          window.location.reload();
        });
      //  console.log(this);

      console.log("devour");
    });

    $("#button").on("click", function () {
      var burgerName = $("#input").val()
      //console.log("Burger");
      $.ajax({
        method: "POST",
        url: "api/new",
        data: { burger_name: burgerName }
      })
        .done(function (msg) {
          //alert("Data Saved: " + msg);
          console.log(msg);
          window.location.reload();
        });
    }

    )
  });


</script>